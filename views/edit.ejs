<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900">
    <div class="">
      <!-- Edit Form -->
      <div
        class="mt-32 max-w-md mx-auto p-4 bg-gray-800 shadow-outline rounded-lg shadow-md"
      >
        <h2 class="text-gray-200 text-2xl mb-4">Edit Post</h2>
        <form method="POST" action="/posts/update/<%= post._id %>">
          <div class="mb-4">
            <label for="id" class="block text-gray-200 text-sm font-bold mb-2"
              >ID</label
            >
            <input
              type="text"
              id="id"
              value="<%= post._id %>"
              name="_id"
              class="bg-gray-700 text-gray-200 p-2 w-full rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-400"
            />
          </div>
          <div class="mb-4">
            <label
              for="postData"
              class="block text-gray-200 text-sm font-bold mb-2"
              >Post Data</label
            >
            <input
              type="text"
              id="postData"
              name="postData"
              value="<%= post.postData %>"
              class="bg-gray-700 text-gray-200 p-2 w-full rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-400"
            />
          </div>
          <div class="mb-4">
            <label
              for="content"
              class="block text-gray-200 text-sm font-bold mb-2"
              >Content</label
            >
            <input id="content" value="<%= post.content %>" name="content"
            class="bg-gray-700 text-gray-200 p-2 w-full rounded-lg
            focus:outline-none focus:ring-2 focus:ring-gray-400" / >
          </div>
          <div class="flex justify-between">
            <input
              type="submit"
              class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg"
              value="Update Post"
            />

            <a
              href="/users/profile"
              type="button"
              class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg"
            >
              Cancel
            </a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
